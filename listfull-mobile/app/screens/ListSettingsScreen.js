import React from 'react';
import { connect } from 'react-redux';
import { Text, View, Button } from 'react-native';

class ListSettingsScreen extends React.Component {
	render() {
		return (
			<Text>
				HERE
			</Text>
		);
	}
}

export default connect()(ListSettingsScreen);
